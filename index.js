'use strict';

{
  const product = 'sofa';
  const amount = 5;
  const category = 'furniture';
  const price = 7000;

  console.log('product: ', product);
  console.log(`общая сумма товара: ${amount * price}`);
}

{
  const product = 'table';
  const amount = 20;
  const category = 'furniture';
  const price = 3500;

  console.log('product: ', product);
  console.log(`общая сумма товара: ${amount * price}`);
}